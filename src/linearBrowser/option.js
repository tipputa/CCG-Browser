import * as col from "../utils/color"
const size = {
    width: 0,
    height: 0,
    left: 0,
    top: 0,
    right: 0,
    bottom: 0,
}

const font = {
    fontSize: 13
}

export const options = {
    elementId: "def",
    setSize: false,
    height: 600,
    width: 600,
    each
}

const color = {
    colorScheme: col.TAYLOR_COLOR,
}